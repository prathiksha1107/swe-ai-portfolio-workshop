# Completed Immersive Tech Example

This folder shows how the human-approved Prathiksha Ravi Krishnan profile looks in
Template 2. It is separate from the clean reusable starter.

- `PROFILE.md` is the approved factual source.
- `index.html` contains the personalized public content.
- `style.css` and `script.js` preserve Template 2's immersive design and motion.
- `assets/` contains only the template's asset guidance; no public resume is included.

Open `index.html` in a browser to preview the example. The contact information in
this folder was explicitly approved for this example and must not be copied into
another participant's portfolio.
