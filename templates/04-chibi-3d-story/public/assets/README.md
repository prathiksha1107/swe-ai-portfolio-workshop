# Public assets

Place an approved public resume here as `resume.pdf`. The centralized profile data
must also set `resumeUrl` before a Resume link appears.

An optional social-preview image can be stored here after the final branding and
public URL are approved.
